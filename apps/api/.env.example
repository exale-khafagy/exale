# MongoDB Atlas connection string - get from https://cloud.mongodb.com
# Format: mongodb+srv://USER:PASSWORD@cluster.mongodb.net/DATABASE?retryWrites=true&w=majority
DATABASE_URL=

# CORS allowed origins (comma-separated). Must include dashboard when using dashboard.localhost:3000.
CORS_ORIGIN="http://localhost:3000,http://dashboard.localhost:3000,https://exale.net,https://dashboard.exale.net"

# Clerk (for Admin Guard) - get from https://dashboard.clerk.com
CLERK_SECRET_KEY=

# Server port
PORT=3002

# Optional: Resend API key (email notifications for Contact/Apply)
# RESEND_API_KEY=re_...
# Optional: Email address to receive notifications (defaults to founder)
# NOTIFY_EMAIL=notifications@example.com
# Optional: Verified "from" address for production (e.g. Exale <noreply@exale.net>)
# EMAIL_FROM=Exale <noreply@exale.net>
